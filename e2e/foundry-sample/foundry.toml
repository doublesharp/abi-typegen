[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.34"

[abi-typegen]
# Generate from source fixtures, excluding the generated interfaces compiled
# alongside them by Forge.
contracts = ["Token", "Vault", "Registry", "EdgeCases", "Exchange", "TupleCases"]
